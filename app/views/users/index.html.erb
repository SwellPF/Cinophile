<h1>List of Users</h1>
<ul>
<% @users.each do |user| %>
<li>
    <%= user.name %> <%= link_to "Edit", edit_user_path(user), class: "button" %> <%= link_to "Delete", user, method: :delete, data: { confirm: "Confirm deletion?" }, class: "button" %>
</li>
<% end %>
</ul>
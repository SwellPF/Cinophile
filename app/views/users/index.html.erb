<h1><u>List of Users</u></h1>
<ul>
<% @users.each do |user| %>
<li>
    <%= user.name %>  <button> <%= link_to "Edit", edit_user_path(user) %> </button>  <button> <%= link_to "Delete", user, method: :delete, data: { confirm: "Confirm deletion?" } %></button>
</li>
<% end %>
</ul>